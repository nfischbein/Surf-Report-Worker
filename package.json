{
  "name": "surf-report-worker",
  "version": "0.0.1",
  "private": true,
  "description": "Backend service for AI Surf Report Builder. Fetches upstream marine data server-side and serves clean normalized JSON to daughter prompts.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}